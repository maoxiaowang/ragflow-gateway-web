import {AppShell, Text, Button, Combobox, Box, Menu} from '@mantine/core';
import AppLayout from "@/layouts/AppLayout";

export default function Dashboard() {
  return (
    <AppLayout>
      ...
    </AppLayout>
  );
}